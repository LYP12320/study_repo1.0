聚类算法——K-MEANS
基本概念：
    k值：需要得到的簇的个数，需要指定值。

    质心：均值，即各向量各维取平均即可。

    距离的度量：常用欧几里得距离和余弦相似度（先标准化）

    优化目标：min∑(i=1...k)∑(x∈c)dist(c,x)**2

工作流程：1、指定k值
         2、随机选择质心，遍历数据进行归类。
         3、更新质心，在归类。
         4、不断重复3直至数据类别不在变动。


优势：简单，快速，适合常规数据集。

劣势： K值难以确定。
      复杂度与样本呈线性关系（因为每次更新质心都会遍历所有数据。）。
      很难发现任意形状的簇。

K-MEANS工作流程示意网址：https://www.naftaliharris.com/blog/visualizing-k-means-clustering


注意：K-MEANS算法其初始质心值对其影响很大，所以往往每次的聚类结果会有很大差别，建议进行多次聚类观察结果。